function [] = task6_error_decoding()
% for each (ε,δ) pair, evaluate reliability in terms of error rate
% though we know that there can be no security for δ < ε
values = [0.001 0.002 0.005 0.01 0.02 0.05 0.1 0.15 0.2 0.25 0.3 0.4 0.5];
results = zeros(size(values,2),size(values,2));

for e=1:size(values,1)
    for d=1:size(values,1)
        %
    end
end

end

